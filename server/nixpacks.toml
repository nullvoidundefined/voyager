[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd /app/server && npm install --production=false"]

[phases.build]
cmds = ["cd /app/server && npx tsc && npx tsc-alias -p tsconfig.json"]

[start]
cmd = "cd /app/server && node dist/index.js"
